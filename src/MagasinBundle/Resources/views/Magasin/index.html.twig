{% extends "/template/base.html.twig" %}

{% block title %}
Magasins Shoppy
{% endblock title %}

{% block body %}
<h3>Bienvenue à la page des magasins</h3>

    <table border="1">
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Taille du stock</th>
            <th>matricule fiscal</th>
            <th>Actions</th>

        </tr>
        {% for elem in data  %}
            <tr>

                <td>{{ elem.id }}</td>
                <td>{{elem.nom}}</td>
                <td>{{ elem.tailleStock}}</td>
                <td>{{ elem.matriculeFiscal}}</td>
                <td><a href="{{path('magasin_delete',{'id':elem.id})}}"><button>Supprimer</button><a/>
                        <a href="{{path('magasin_update',{'id':elem.id})}}"><button>Modifier</button><a/></td>
            </tr>
        {% endfor %}
    </table>
    <td><a href={{ path('magasin_ajouter') }}><button>Ajouter magasin</button><a/></td>

{% endblock body %}